export const LEVEL_GEN_DEFAULTS = Object.freeze({
  profile: "rectangular",
  edgeBias: false,
  minDistance: 0,
  requirePath: true,
});

export const AFFINITY_DEFAULTS = Object.freeze({
  manaCost: 0,
  stacks: 1,
});
