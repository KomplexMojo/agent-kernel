# Documentation.md

Status tracker and decision log for this deliverable.

## Stitch UI Rewrite Handoff Status (2026-03-28)

- Source plan: `[Spec](intent://local/note/spec)` (`M0 -> M7`)
- Current checkpoint: `M0` through `M7` implemented on the repaired `main` baseline; the regression ring is green again for continued Stitch work.
- Handoff state: original Stitch rewrite milestones are complete on `main`; additional work should start from a new follow-on milestone instead of reopening `M4` through `M7`.

### Stitch Milestone Snapshot

- M0: Completed
- M1: Completed
- M2: Completed (`go` decision recorded)
- M3: Completed
- M4: Completed
- M5: Completed
- M6: Completed
- M7: Completed (baseline reconciled on `main`)

### M7 Initial Validation Log (2026-03-27, pre-repair)

| Command | Result | Notes |
| --- | --- | --- |
| `pnpm run build:wasm` | Passed | WASM built and copied to `packages/ui-web/assets/core-as.wasm`. |
| `node --test tests/ui-web/*.test.mjs` | Failed | 49 passed / 6 failed. Primary failures: missing `packages/runtime/src/build/orchestrate-build.js`; missing `tests/fixtures/artifacts/budget-artifact-v1-basic.json`. |
| `node --test tests/integration/*.test.js` | Failed | 0 passed / 15 failed / 1 skipped. Failures chain from missing `packages/runtime/src/build/orchestrate-build.js`. |
| `pnpm run serve:ui` smoke | Passed | Server active at `http://localhost:8001`; page check returned HTTP `200` for `/packages/ui-web/index.html`. |

### M7 Documentation Sync

- Updated `docs/human-interfaces.md` to reflect current tab flow: `Design -> Preview -> Run -> Diagnostics`, and to reference browser command-host build execution.
- Updated `docs/README.md` quickstart summary to match the same active workflow tab order.
- Updated `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Implement.md`, and this file with Stitch rewrite execution/handoff tracking.

### M7 Historical Blockers (Resolved on 2026-03-27 PDT)

1. `packages/runtime/src/commands/kernel.js` imports `../build/orchestrate-build.js`, but `packages/runtime/src/build/orchestrate-build.js` does not exist.
2. `tests/ui-web/budget-panels.test.mjs` references `tests/fixtures/artifacts/budget-artifact-v1-basic.json`, but that fixture file is missing.

### Baseline Reconciliation (2026-03-27 PDT)

- Resolved the missing tracked-file/source-control drift on `main`, including the restored runtime build/configurator sources and the missing negative fixture required by adapters-cli validation.
- Rebaselined stale MVP movement, configurator, affinity-summary, prompt, and UI expectations so they match the repaired runtime currently on `main`.
- Current checkpoint for continuing the Stitch spec: the baseline regression ring on `main` is green again, which closes the original M7 handoff blockers.

Validation after reconciliation:
- `node --test tests/runtime/*.test.js` -> passed (`120` passed)
- `node --test tests/adapters-cli/*.test.js` -> passed (`102` passed)
- `node --test tests/adapters-web/*.test.js` -> passed (`7` passed)
- `node --test tests/integration/*.test.js` -> passed (`15` passed, `1` skipped live LLM test)
- `node --test tests/ui-web/*.test.mjs` -> passed (`83` passed)

### M7 Final Validation Status (2026-03-28)

- `pnpm run build:wasm` -> passed
- `node --test tests/ui-web/*.test.mjs` -> passed (`83` passed)
- `node --test tests/integration/*.test.js` -> passed (`15` passed, `1` skipped live LLM test)
- `pnpm run serve:ui` smoke -> last confirmed during the original M7 ring on 2026-03-27 (`/packages/ui-web/index.html` returned HTTP `200`)

## 1) Current Status

- Deliverable: Aligning UI features with CLI
- Owner: Darren
- Start date: 2026-03-01
- Target date: [YYYY-MM-DD]
- Overall status: Completed for branch close (shared-rails parity scope is validated; deeper IPFS/blockchain/live-runtime-LLM product work is intentionally split to follow-on branches)

Branch-close gate (items 1-5, completed):
1. Remaining parity gaps marked `partial` for active UI/CLI flows were normalized to their real implemented or UI-only closure state.
2. Shared-rails hooks/contracts for in-scope commands/adapters are confirmed; deeper IPFS/blockchain/LLM product work is moved to follow-on branches.
3. Unnecessary dependency coupling from the default workflow is removed or isolated in the browser-first/minimal-install posture.
4. Final regression baselines are recorded below.
5. Final documentation and validation package is complete.

Milestone status:
- M1 (inventory + matrix): Completed (full UI inventory with severity + milestone mapping)
- M2 (direct authoring parity): Completed (Room, Attacker, and Defender direct CLI authoring parity implemented and documented)
- M3 (remaining deterministic parity gaps + baseline cleanup): Completed (matrix normalized; previously tracked baseline failures rebaselined/green)
- M4 (shared-rails unification + dependency reduction): Completed (active UI flow is on shared rails; standalone adapter commands plus IPFS/blockchain hooks and runtime LLM capture/replay/inspect surfaces share kernel/browser rails; deeper productization is split to follow-on branches)
- M5 (final verification + branch handoff): Completed (validation package recorded; docs normalized to final closure scope)
- U1 (Node-independent command kernel extraction): Completed (`build`/`solve`/`run`/`replay`/`inspect`/`configurator`/`budget`/`llm-plan` now run via shared runtime command kernel)
- U2 (browser worker command host): Completed (`cli-worker` adapter added with worker + in-process execution for browser-hosted shared-kernel command flows, including `solve` plus runtime artifact commands)
- U3 (UI build/planning flows routed through command host): Completed (Design, Pool, Ollama, and Diagnostics build flows now use the browser command host)
- U4 (shared adapter consolidation): Completed (standalone adapter commands plus IPFS/blockchain/LLM hooks are aligned on shared rails; deeper productization is intentionally branched off)
- U5 (legacy/direct-path removal in active scope): Completed (active `/bridge/build` path is removed; remaining environment-specific/direct adapter scope is outside the default workflow and bounded to optional follow-on branches)
- U6 (Node-vs-browser artifact equivalence suite): Completed (shared artifact/runtime commands `build`/`solve`/`run`/`replay`/`inspect`/`configurator`/`budget`/`llm-plan` now have canonical equivalence tests against Node CLI output)
- U7 (docs + architecture close-out): Completed (single-rails and optional-scope story is now explicit)
- U8 (minimum-install validation): Completed (default Design -> Preview -> Run baseline is documented as browser-hosted, fixture-first, and not dependent on live IPFS/blockchain/Ollama services)

### Branch-Close Summary (2026-03-21)

Completed on this branch so far:
- direct CLI parity for Room, Attacker, and Defender authoring
- shared command kernel for core artifact/runtime commands
- browser command host for shared-kernel commands
- active Design/Preview/Run build flow routed through the shared browser host
- Node-vs-browser equivalence coverage for the shared-kernel command set
- substantial UI cleanup so the active interface follows the current workflow
- minimum-install baseline for the default workflow is now documented as browser-hosted and fixture-first, with live IPFS/blockchain/Ollama services explicitly optional

Branch-close result:
- all remaining parity rows are now classified as `implemented` or `ui-only`
- shared hooks/contracts for `ipfs`, `blockchain`, and `llm` are confirmed in the main code
- the default Design -> Preview -> Run workflow is browser-hosted, fixture-first, and not dependent on optional live services
- the final regression package is green, with one intentional skipped live integration test
- deeper IPFS/blockchain/live-runtime-LLM product work is explicitly follow-on branch scope

### Final Validation

- `pnpm run build:wasm`
- `node --test tests/runtime/*.test.js`
- `node --test tests/ui-web/*.test.mjs`
- `node --test tests/integration/*.test.js`
- `node --test tests/adapters-cli/*.test.js`
- `node --test tests/adapters-web/*.test.js`
- Results:
  - runtime: 101 passed
  - ui-web: 77 passed
  - integration: 15 passed, 1 skipped (intentional live test)
  - adapters-cli: 102 passed
  - adapters-web: 7 passed

## 2) Change Log

| Date | Change | Files | Validation |
| --- | --- | --- | --- |
| 2026-04-08 | Repaired the remaining UI parity gap for agent-authored bundle ingress: shared a merged BuildSpec card-set reader across Preview and inspector flows, normalized agent-authoring singleton fields on Diagnostics bundle/build ingress, and added Design hydration for file/IPFS-loaded authored bundles so editing can continue without manual JSON conversion | `packages/ui-web/src/build-spec-ui.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/src/views/preview-view.js`, `packages/ui-web/src/bundle-review.js`, `packages/ui-web/src/build-orchestrator.js`, `packages/ui-web/src/main.js`, `packages/ui-web/src/actor-inspector.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/preview-view.test.mjs`, `tests/ui-web/bundle-integration.test.mjs`, `tests/integration/ui-cli-equivalence.test.js` | `node --test tests/ui-web/view-wiring.test.mjs`, `node --test tests/ui-web/actor-inspector.test.mjs`, `node --test tests/ui-web/preview-view.test.mjs tests/ui-web/bundle-integration.test.mjs`, `node --test --test-name-pattern='wireDesignView hydrates editor cards from an agent-authored build spec' tests/ui-web/design-view.test.mjs`, `node --test tests/integration/ui-cli-equivalence.test.js` |
| 2026-03-27 | Reconciled the repaired `main` baseline after the missing-file recovery: refreshed MVP movement goldens, restored a valid inline configurator build spec plus budget/price-list fixtures, rebaselined affinity/prompt/UI expectations, and cleared the remaining 14 non-source-control regressions | `tests/fixtures/artifacts/action-sequence-v1-mvp-to-exit.json`, `tests/fixtures/artifacts/sim-config-artifact-v1-mvp-grid.json`, `tests/fixtures/artifacts/frame-buffer-log-v1-mvp.json`, `tests/fixtures/artifacts/build-spec-v1-configurator.json`, `tests/fixtures/artifacts/budget-artifact-v1-basic.json`, `tests/fixtures/artifacts/price-list-artifact-v1-basic.json`, `tests/fixtures/artifacts/initial-state-artifact-v1-configurator-affinity.json`, `tests/fixtures/personas/affinity-resolution-v1-basic.json`, `tests/fixtures/e2e/llm-summary-response.json`, `tests/runtime/configurator-startup.test.js`, `tests/runtime/runtime-persona-schedule.test.js`, `tests/ui-web/design-view.test.mjs` | `node --test tests/runtime/*.test.js`, `node --test tests/adapters-cli/*.test.js`, `node --test tests/adapters-web/*.test.js`, `node --test tests/integration/*.test.js`, `node --test tests/ui-web/*.test.mjs` |
| 2026-03-21 | Finalized branch-close documentation: normalized the parity matrix to the real implemented/UI-only end state, marked the branch-close gates complete, and logged the final validation package for branch closure | `local-codex/ui-cli-parity-matrix.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-unification-plan.md` | `pnpm run build:wasm`, `node --test tests/runtime/*.test.js`, `node --test tests/ui-web/*.test.mjs`, `node --test tests/integration/*.test.js`, `node --test tests/adapters-cli/*.test.js`, `node --test tests/adapters-web/*.test.js` |
| 2026-03-21 | Narrowed branch-close scope so `ipfs`, `blockchain`, and deeper live-runtime `llm` product work move to follow-on branches once shared hooks are present; updated the branch artifacts and public unification docs to reflect the hook-first boundary | `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-unification-plan.md`, `docs/README.md` | `node --test tests/runtime/*.test.js`, `node --test tests/ui-web/*.test.mjs`, `node --test tests/integration/*.test.js`, `node --test tests/adapters-cli/*.test.js`, `node --test tests/adapters-web/*.test.js` |
| 2026-03-21 | Completed `B4-S1` minimum-install baseline documentation/validation: documented the default workflow as browser-hosted and fixture-first, explicitly marked live IPFS/blockchain/Ollama services as optional, and aligned the branch artifacts with that baseline story | `docs/cli-runbook.md`, `docs/README.md`, `docs/reference-handout.md`, `packages/adapters-cli/README.md`, `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-unification-plan.md` | `node --test tests/ui-web/design-view.test.mjs`, `node --test tests/ui-web/preview-view.test.mjs`, `node --test tests/adapters-web/cli-worker.test.js`, `node --test tests/integration/ui-cli-equivalence.test.js`, `pnpm run build:wasm` |
| 2026-03-21 | Completed `B2-S3C` runtime fallback policy lock: default runtime policy no longer implies automatic solver->LLM fallback, and solver-unfulfilled paths now report explicit non-performed fallback status on the existing rail | `packages/runtime/src/personas/_shared/runtime-decision.js`, `packages/runtime/src/personas/_shared/runtime-decision.mts`, `packages/runtime/src/personas/_shared/tick-orchestrator.js`, `packages/runtime/src/personas/_shared/tick-orchestrator.mts`, `tests/runtime/runtime-decision-contract.test.js`, `tests/personas/tick-orchestrator.test.js`, `local-codex/runtime-reasoning-contract.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/ui-cli-unification-plan.md` | targeted runtime/persona + parity regression sweeps passed |
| 2026-03-21 | Completed `B2-S3B` runtime LLM lifecycle surfacing slice: runtime decision captures are now emitted as canonical run artifacts and surfaced in inspect/replay summaries on shared rails | `packages/runtime/src/commands/run-helpers.js`, `packages/runtime/src/commands/kernel.js`, `packages/adapters-web/src/adapters/cli-worker/shared.js`, `tests/runtime/run-helpers-runtime-decision.test.js`, `tests/runtime/command-kernel-inspect-runtime-decision.test.js`, `tests/adapters-cli/ak.test.js`, `tests/adapters-web/cli-worker.test.js`, `tests/integration/ui-cli-equivalence.test.js`, `packages/adapters-cli/README.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md` | targeted runtime/cli/web/integration sweeps passed |
| 2026-03-21 | Completed `B2-S3A` shared-rails LLM command parity slice: Ollama prompt panel now prefers browser command-host `llm` execution (with fallback only when host is unavailable), and standalone `llm` now has explicit Node-vs-browser artifact equivalence coverage | `packages/ui-web/src/ollama-panel.js`, `tests/ui-web/ollama-panel.test.mjs`, `tests/integration/ui-cli-equivalence.test.js`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md` | targeted and broad ui-web/adapters-web/integration sweeps passed |
| 2026-03-21 | Completed `B2-S2` blockchain mint/load shared-rails slice: added canonical `blockchain-mint`/`blockchain-load` contracts in kernel/CLI/browser host, rewired Design `Mint` to shared rails, and added load-minted-by-token support in the Design workflow | `packages/adapters-cli/src/adapters/blockchain/index.js`, `packages/adapters-web/src/adapters/blockchain/index.js`, `packages/runtime/src/commands/kernel.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-web/src/adapters/cli-worker/shared.js`, `packages/adapters-web/src/adapters/cli-worker/index.js`, `packages/ui-web/src/design-guidance.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/index.html`, `tests/adapters-cli/ak-adapter-commands.test.js`, `tests/adapters-cli/ak-errors.test.js`, `tests/adapters-cli/adapter-modules.test.js`, `tests/adapters-web/adapter-modules.test.js`, `tests/adapters-web/cli-worker.test.js`, `tests/ui-web/design-view.test.mjs`, `tests/integration/ui-cli-equivalence.test.js`, `packages/adapters-cli/README.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/ui-cli-unification-plan.md` | adapters-cli/adapters-web/ui-web/integration sweeps passed |
| 2026-03-21 | Completed `B2-S1B` IPFS canonical publish/storage write-path on shared rails: added `ipfs-publish` kernel/CLI/browser-host flows, added Diagnostics publish-from-canonical-bundle behavior (CID empty => publish), and extended Node-vs-browser equivalence coverage to `ipfs-publish` + `ipfs-load` | `packages/adapters-cli/src/adapters/ipfs/index.js`, `packages/adapters-web/src/adapters/ipfs/index.js`, `packages/runtime/src/commands/kernel.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-web/src/adapters/cli-worker/shared.js`, `packages/adapters-web/src/adapters/cli-worker/index.js`, `packages/ui-web/src/adapter-panel.js`, `packages/ui-web/src/views/diagnostics-view.js`, `packages/ui-web/src/build-orchestrator.js`, `packages/ui-web/src/bundle-review.js`, `tests/adapters-cli/ak-adapter-commands.test.js`, `tests/adapters-cli/adapter-modules.test.js`, `tests/adapters-web/adapter-modules.test.js`, `tests/adapters-web/cli-worker.test.js`, `tests/ui-web/adapter-playground.test.mjs`, `tests/integration/ui-cli-equivalence.test.js`, `packages/adapters-cli/README.md`, `local-codex/Plan.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/ui-cli-unification-plan.md` | adapters-cli/adapters-web/ui-web/integration sweeps passed |
| 2026-03-21 | Completed `B2-S1A` IPFS artifact reload on shared rails: added kernel/browser/CLI `ipfs-load` command paths and wired Diagnostics IPFS loads into Bundle Review so canonical bundle+manifest payloads can be restored from CID-backed artifacts | `packages/runtime/src/commands/kernel.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-web/src/adapters/cli-worker/shared.js`, `packages/adapters-web/src/adapters/cli-worker/index.js`, `packages/ui-web/src/adapter-panel.js`, `packages/ui-web/src/views/diagnostics-view.js`, `packages/ui-web/src/bundle-review.js`, `tests/adapters-cli/ak-adapter-commands.test.js`, `tests/adapters-web/cli-worker.test.js`, `tests/ui-web/adapter-playground.test.mjs`, `tests/fixtures/adapters/ipfs-artifacts-map.json`, `packages/adapters-cli/README.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Plan.md`, `local-codex/Documentation.md` | adapters-cli/adapters-web/ui-web/integration sweeps passed |
| 2026-03-21 | Completed shared-rails adapter-command slice (`B2-S0`): moved standalone `ipfs`/`blockchain`/`llm` command execution onto shared command-kernel rails, exposed them through browser command hosting, and wired the Diagnostics adapter panel to prefer command-host execution | `packages/runtime/src/commands/kernel.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-web/src/adapters/cli-worker/shared.js`, `packages/adapters-web/src/adapters/cli-worker/index.js`, `packages/ui-web/src/adapter-panel.js`, `packages/ui-web/src/views/diagnostics-view.js`, `tests/adapters-web/cli-worker.test.js`, `tests/ui-web/adapter-playground.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Plan.md`, `local-codex/Documentation.md` | adapters-cli/adapters-web/ui-web/integration sweeps passed |
| 2026-03-21 | Completed branch-close baseline cleanup slice (`B5-S1`) and diagnostics budget-panel parity slice: rebaselined stale LLM/level-builder expectations, wired Diagnostics budget panels to canonical build/bundle artifacts, and updated integration prompt fixture drift | `tests/adapters-cli/ak-llm-plan.test.js`, `tests/adapters-web/adapter-modules.test.js`, `tests/fixtures/e2e/llm-summary-response.json`, `packages/ui-web/src/budget-panels.js`, `packages/ui-web/src/views/diagnostics-view.js`, `tests/ui-web/budget-panels.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted + broad adapter/ui/integration regressions passed |
| 2026-03-21 | Synced branch-close policy with `Plan.md`: added an explicit 1-5 branch-close gate and mapped the current checkpoint requirements to those five required items | `local-codex/Documentation.md`, `local-codex/Plan.md` | Docs-only change |
| 2026-03-21 | Added a repo-local model selection cheat sheet for the remaining branch work so each slice can be started with an explicit model and reasoning choice instead of defaulting to an expensive frontier setting | `local-codex/MODEL-SELECTION-CHEATSHEET.md`, `local-codex/Documentation.md` | Docs-only change |
| 2026-03-15 | End-of-day documentation close-out: refreshed the execution artifacts and public docs so tomorrow can resume from the real branch checkpoint (`B2-S1` next, `B2-S4` checkpoint locked, shared-rails and minimum-install work still open) | `local-codex/Plan.md`, `local-codex/Implement.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/ui-cli-unification-plan.md`, `docs/README.md`, `docs/architecture/diagram.mmd` | Docs-only change |
| 2026-03-15 | Implemented explicit manual-mode live Ollama runtime fulfillment on the existing reasoning rail: `solver_request` entries can now be fulfilled by the local LLM adapter when provider policy explicitly opts into manual non-deterministic mode, with `CapturedInputArtifact` recording and normalized action enactment in the tick orchestrator | `packages/runtime/src/personas/_shared/runtime-decision.js`, `packages/runtime/src/personas/_shared/runtime-decision.mts`, `packages/runtime/src/personas/_shared/tick-orchestrator.js`, `packages/runtime/src/personas/_shared/tick-orchestrator.mts`, `packages/runtime/src/runner/runtime-fsm.mjs`, `packages/runtime/src/personas/actor/controller.js`, `packages/runtime/src/personas/actor/controller.mts`, `packages/runtime/src/personas/actor/README.md`, `tests/personas/actor-runtime-decision.test.js`, `tests/personas/tick-orchestrator.test.js`, `local-codex/runtime-reasoning-contract.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted runtime/persona/CLI regressions passed |
| 2026-03-15 | Wired `B2-S4` into real gameplay and runtime summaries: actor persona now emits `runtime-decision-v1` solver requests from live observation/candidate-action context, `replay` / `inspect` surface normalized runtime decisions, and live Ollama runtime policy is codified as explicit manual non-deterministic mode only | `packages/runtime/src/personas/actor/controller.js`, `packages/runtime/src/personas/actor/controller.mts`, `packages/runtime/src/personas/_shared/runtime-decision.js`, `packages/runtime/src/personas/_shared/runtime-decision.mts`, `packages/runtime/src/commands/run-helpers.js`, `packages/runtime/src/commands/kernel.js`, `tests/personas/actor-runtime-decision.test.js`, `tests/runtime/run-helpers-runtime-decision.test.js`, `tests/runtime/command-kernel-inspect-runtime-decision.test.js`, `tests/runtime/runtime-decision-contract.test.js`, `tests/adapters-cli/ak.test.js`, `local-codex/runtime-reasoning-contract.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted actor/runtime/CLI regressions passed |
| 2026-03-15 | Implemented the first `B2-S4` runtime slice on the existing rail: added a shared `runtime-decision-v1` helper, normalized solver/capture decisions into validated `Action` outputs, and wired solver-selected actions into the tick orchestrator | `packages/runtime/src/personas/_shared/runtime-decision.js`, `packages/runtime/src/personas/_shared/runtime-decision.mts`, `packages/runtime/src/personas/_shared/tick-orchestrator.js`, `packages/runtime/src/personas/_shared/tick-orchestrator.mts`, `packages/runtime/src/personas/orchestrator/llm-capture.js`, `tests/runtime/runtime-decision-contract.test.js`, `tests/personas/tick-orchestrator.test.js`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted runtime/persona/capture regressions passed |
| 2026-03-15 | Reframed `B2-S4` around the existing solver/capture pipeline: `runtime-decision-v1` is now an embedded payload contract carried through `SolverRequest`, `SolverResult`, and `CapturedInputArtifact` rather than new top-level runtime decision artifacts | `local-codex/runtime-reasoning-contract.md`, `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Implement.md`, `local-codex/ui-cli-unification-plan.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Docs-only change |
| 2026-03-15 | Added solver-backed runtime reasoning to active branch scope as a shared-rails gameplay capability, distinct from but paired with structured local-first LLM runtime decisions | `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/ui-cli-unification-plan.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Docs-only change |
| 2026-03-15 | Decided that `ipfs`, `blockchain`, and `llm` remain in active shared-rails scope, updated the parity matrix to reflect their product intent, and queued concrete implementation slices (`B2-S1`/`B2-S2`/`B2-S3`) | `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/ui-cli-unification-plan.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Docs-only change |
| 2026-03-15 | Completed `B1-S1` by rebaselining configurator artifact fixtures to the current deterministic room-aware output and closing the Design budget split / BuildSpec dispatch parity row | `tests/fixtures/artifacts/sim-config-artifact-v1-configurator-trap.json`, `tests/fixtures/artifacts/initial-state-artifact-v1-configurator-affinity.json`, `tests/personas/configurator-artifact-fixtures.test.js`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Plan.md`, `local-codex/Documentation.md` | Targeted CLI/browser/UI parity suite passed |
| 2026-03-15 | Defined the first concrete branch-close implementation slice (`B1-S1`) around Design budget split / BuildSpec dispatch parity and the `ak-configurator` layout drift | `local-codex/Plan.md`, `local-codex/Documentation.md` | Docs-only change |
| 2026-03-15 | Rebased the local-codex artifacts on the true branch checkpoint: parity remains active, shared-rails completion is still open, and the minimum-install goal is now explicit in the execution docs | `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Implement.md`, `local-codex/ui-cli-unification-plan.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Docs-only change |
| 2026-03-01 | Initialized deliverable-specific planning artifacts for UI-to-CLI parity | `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Implement.md`, `local-codex/CODEX-CHEATSHEET.md` | Docs-only change |
| 2026-03-01 | Completed Room-focused parity audit and populated matrix with concrete status rows | `local-codex/ui-cli-parity-matrix.md`, `local-codex/Plan.md`, `local-codex/Documentation.md` | Code audit only (no runtime behavior change) |
| 2026-03-01 | Completed architecture review for UI-as-CLI-wrapper and created unification plan | `local-codex/ui-cli-unification-plan.md`, `local-codex/Plan.md` | Code audit only (no runtime behavior change) |
| 2026-03-01 | Captured "no legacy preservation" execution policy across Codex artifacts | `local-codex/Prompt.md`, `local-codex/Plan.md`, `local-codex/Implement.md`, `local-codex/ui-cli-unification-plan.md`, `docs/implementation-plans/aligning-UI-features-with-CLI.md` | Docs-only change |
| 2026-03-01 | Completed M1 parity inventory across Design, Simulation, Runtime, and Diagnostics with severity and milestone assignment | `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Code audit commands executed; docs-only change |
| 2026-03-07 | Implemented Room CLI parity (RP1-RP3) with direct authoring command, defaults, multi-room support, and validation tests | `packages/adapters-cli/src/cli/ak-impl.mjs`, `tests/adapters-cli/ak-room-plan.test.js`, `packages/adapters-cli/README.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted CLI tests passed |
| 2026-03-07 | Implemented RP4 room cost-accounting E2E coverage for `room-plan` budget receipts | `packages/adapters-cli/src/cli/ak-impl.mjs`, `tests/adapters-cli/ak-room-plan.test.js`, `packages/adapters-cli/README.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Room-plan budget receipt integration tests passed |
| 2026-03-07 | Implemented AP1 attacker CLI parity with direct authoring command, defaults, multi-attacker support, and validation tests | `packages/adapters-cli/src/cli/ak-impl.mjs`, `tests/adapters-cli/ak-attacker-plan.test.js`, `packages/adapters-cli/README.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted CLI tests passed |
| 2026-03-07 | Implemented DP1 defender CLI parity with direct authoring command, defaults, multi-defender support, and validation tests | `packages/adapters-cli/src/cli/ak-impl.mjs`, `tests/adapters-cli/ak-defender-plan.test.js`, `packages/adapters-cli/README.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted CLI tests passed |
| 2026-03-07 | Implemented AP2/DP2 advanced attacker/defender CLI parity for affinities (expression+stacks), vitals, and receipt propagation | `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/runtime/src/personas/director/buildspec-assembler.js`, `tests/adapters-cli/ak-attacker-plan.test.js`, `tests/adapters-cli/ak-defender-plan.test.js`, `packages/adapters-cli/README.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted CLI + runtime tests passed |
| 2026-03-08 | Completed AD1 by documenting runnable end-to-end parity recipes for Room, Attacker, and Defender flows and validating command execution | `packages/adapters-cli/README.md`, `local-codex/Documentation.md`, `local-codex/ui-cli-parity-matrix.md`, `docs/implementation-plans/aligning-UI-features-with-CLI.md` | Recipe command runs passed |
| 2026-03-08 | Completed U1 by extracting a runtime command kernel for `build`, `configurator`, `budget`, and `llm-plan`, and rewiring CLI handlers to delegate through injected host services | `packages/runtime/src/commands/kernel.js`, `packages/runtime/src/index.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `tests/runtime/command-kernel-budget.test.js`, `local-codex/Documentation.md` | Targeted CLI/runtime command tests passed (with existing known baseline failures noted) |
| 2026-03-11 | Completed U2/U3 active-path browser command hosting by adding `cli-worker`, routing Design/Pool/Ollama/Diagnostics build flows through it, and removing the `/bridge/build` server path | `packages/runtime/src/commands/ui-flow.js`, `packages/adapters-web/src/adapters/cli-worker/**`, `packages/ui-web/src/build-orchestrator.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/src/pool-flow.js`, `packages/ui-web/src/ollama-panel.js`, `packages/ui-web/src/views/diagnostics-view.js`, `packages/ui-web/src/main.js`, `packages/ui-web/index.html`, `scripts/serve-ui.mjs`, `tests/adapters-web/cli-worker.test.js`, `tests/ui-web/*.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted browser-host/UI/CLI regression tests passed; one unrelated `tests/adapters-web/adapter-modules.test.js` baseline failure observed in level-builder expectations |
| 2026-03-11 | Extended browser command hosting to the remaining shared-kernel artifact commands (`configurator`, `budget`, `llm-plan`) and completed U6 with canonical Node-vs-browser artifact equivalence tests | `packages/adapters-web/src/adapters/cli-worker/index.js`, `packages/adapters-web/src/adapters/cli-worker/shared.js`, `tests/adapters-web/cli-worker.test.js`, `tests/integration/ui-cli-equivalence.test.js`, `tests/ui-web/*.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Browser-host regression + equivalence suite passed for shared artifact commands |
| 2026-03-12 | Extracted the next shared command-kernel slice for `run`, `replay`, and `inspect`, extended the browser host to those commands, and added Node-vs-browser artifact equivalence coverage | `packages/runtime/src/commands/kernel.js`, `packages/runtime/src/commands/run-helpers.js`, `packages/runtime/src/commands/wasm-core.js`, `packages/runtime/src/index.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-web/src/adapters/cli-worker/**`, `tests/adapters-web/cli-worker.test.js`, `tests/integration/ui-cli-equivalence.test.js`, `docs/README.md`, `docs/architecture/diagram.mmd`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted CLI/browser/equivalence regression suites passed |
| 2026-03-12 | Extracted `solve` into the shared command kernel, exposed it through the browser host, and added kernel/browser equivalence coverage | `packages/runtime/src/commands/kernel.js`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-web/src/adapters/cli-worker/**`, `tests/runtime/command-kernel-solve.test.js`, `tests/adapters-web/cli-worker.test.js`, `tests/integration/ui-cli-equivalence.test.js`, `docs/README.md`, `docs/architecture/diagram.mmd`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted CLI/browser/equivalence regression suites passed |
| 2026-03-14 | Removed the legacy Game Board/runtime UI branch and unused diagnostics placeholders so the active interface matches the current Design + Diagnostics workflow | `packages/ui-web/index.html`, `packages/ui-web/src/main.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/src/views/diagnostics-view.js`, `packages/ui-web/src/bundle-review.js`, `tests/ui-web/*.test.mjs`, `local-codex/Prompt.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted UI/browser regression suites passed after deleting the unused playback/runtime surface and stale tests |
| 2026-03-14 | Restored an iterative Design -> Preview -> Design loop by rendering bundle artifacts in a new Preview tab and wiring `Build And Load Game` back to that active flow | `packages/ui-web/index.html`, `packages/ui-web/src/main.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/src/views/preview-view.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/persona-tabs-layout.test.mjs`, `tests/ui-web/preview-view.test.mjs`, `tests/ui-web/view-wiring.test.mjs`, `local-codex/Prompt.md`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted UI/browser regression suites passed with Preview driven from canonical build artifacts |
| 2026-03-14 | Locked in single-element Design -> Preview iteration for room-only and attacker-only card sets | `tests/ui-web/design-view.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Added regression coverage proving single-card design sets can still build, load Preview, and retain canonical level-generation artifacts |
| 2026-03-14 | Restored the Game Board/runtime UI while keeping Preview, rewired bundle loads to drive both surfaces from canonical artifacts, and reintroduced the right-rail inspector plus runtime controls on the playing surface | `packages/ui-web/index.html`, `packages/ui-web/src/main.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/src/views/simulation-view.js`, `packages/ui-web/src/actor-inspector.js`, `packages/ui-web/src/movement-ui.js`, `tests/ui-web/*.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted UI regression plus browser-host/equivalence guardrail suites passed after restoring the interactive Game Board surface without reintroducing the old bridge path |
| 2026-03-14 | Removed the redundant Selected Actor View so actor targeting and actions are driven directly from the playing surface plus the right-rail inspector | `packages/ui-web/index.html`, `packages/ui-web/src/main.js`, `packages/ui-web/src/views/simulation-view.js`, `tests/ui-web/persona-tabs-layout.test.mjs`, `tests/ui-web/layout-hierarchy.test.mjs`, `tests/ui-web/view-wiring.test.mjs`, `tests/ui-web/simulation-view.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted UI/browser regression suites passed after deleting the duplicate viewport layer and keeping runtime controls attached to the selected inspector entity |
| 2026-03-14 | Reordered the active workflow to Design -> Preview -> Game Board, moved `Build And Load Game` onto the Preview surface, and refreshed Preview from the current design bundle on tab open | `packages/ui-web/index.html`, `packages/ui-web/src/main.js`, `packages/ui-web/src/views/design-view.js`, `packages/ui-web/src/views/preview-view.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/preview-view.test.mjs`, `tests/ui-web/persona-tabs-layout.test.mjs`, `tests/ui-web/view-wiring.test.mjs`, `local-codex/ui-cli-parity-matrix.md`, `local-codex/Documentation.md` | Targeted UI regression, full UI regression, and browser-host/equivalence suites passed after making Preview the staging step before Game Board |
| 2026-03-14 | Moved Design drop/status messaging into a boxed center-rail status area above the card display, removed the helper sentence, and removed the dead top-of-panel placeholder | `packages/ui-web/index.html`, `packages/ui-web/src/design-guidance.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/persona-tabs-layout.test.mjs`, `local-codex/Documentation.md` | Focused Design/UI regressions and the full `ui-web` suite passed after the status-box cleanup |
| 2026-03-14 | Added `Auto-generate` to the Card Builder so the Design tab can fill the remaining room, attacker, and defender allocations with deterministic test cards | `packages/ui-web/index.html`, `packages/ui-web/src/design-guidance.js`, `packages/ui-web/src/views/design-view.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/persona-tabs-layout.test.mjs`, `tests/ui-web/view-wiring.test.mjs`, `local-codex/Documentation.md` | Focused Design/UI regressions and the full `ui-web` suite passed after adding budget-aware auto-generation |
| 2026-03-14 | Added a Preview launch guard so `Build And Load Game` refuses partial designs that do not include at least one room, attacker, and defender | `packages/ui-web/src/views/preview-view.js`, `packages/ui-web/src/main.js`, `tests/ui-web/preview-view.test.mjs`, `local-codex/Documentation.md` | Focused Preview/UI regressions and the full `ui-web` suite passed after enforcing the required authored card mix for Run launch |
| 2026-03-14 | Standardized the central status/error box across Design, Preview, and Run by using the same boxed component above each primary surface and removing Run’s old pill-style status indicator | `packages/ui-web/index.html`, `packages/ui-web/src/views/preview-view.js`, `packages/ui-web/src/views/simulation-view.js`, `tests/ui-web/persona-tabs-layout.test.mjs`, `tests/ui-web/preview-view.test.mjs`, `tests/ui-web/view-wiring.test.mjs`, `tests/ui-web/simulation-view.test.mjs`, `local-codex/Documentation.md` | Focused status/layout regressions and the full `ui-web` suite passed after unifying status placement and info/error level handling |
| 2026-03-14 | Added shared motivation exclusivity groups and paired designer rendering so contradictory motivations are blocked in runtime normalization, UI authoring, and direct CLI parsing | `packages/runtime/src/personas/configurator/motivation-loadouts.js`, `packages/runtime/src/personas/director/summary-selections.js`, `packages/ui-web/src/design-guidance.js`, `packages/ui-web/src/actor-inspector.js`, `packages/ui-web/index.html`, `packages/adapters-cli/src/cli/ak-impl.mjs`, `packages/adapters-cli/README.md`, `packages/runtime/src/personas/actor/README.md`, `tests/runtime/motivation-loadouts.test.js`, `tests/runtime/summary-selections.test.js`, `tests/ui-web/design-guidance-affinity-sync.test.mjs`, `tests/ui-web/design-view.test.mjs`, `tests/adapters-cli/ak-attacker-plan.test.js`, `tests/adapters-cli/ak-defender-plan.test.js`, `local-codex/Documentation.md` | Targeted runtime/UI/CLI regressions plus the full `ui-web` sweep passed after enforcing exclusive motivation families and paired UI affordances |
| 2026-03-14 | Bounded the Run-side Room, Attacker, and Defender inspector lists to a four-row viewport with independent scrollbars | `packages/ui-web/index.html`, `tests/ui-web/persona-tabs-layout.test.mjs`, `local-codex/Documentation.md` | Targeted Run layout and inspector tests passed after tightening the right-rail list scroll behavior |
| 2026-03-14 | Removed the extra selected-instance header line from the Run-side Game Inspector so the right rail only shows the grouped lists and the detail card | `packages/ui-web/index.html`, `packages/ui-web/src/main.js`, `tests/ui-web/persona-tabs-layout.test.mjs`, `local-codex/Documentation.md` | Targeted Run layout, view-wiring, and inspector tests passed after deleting the redundant header status element |
| 2026-03-14 | Removed the duplicated card-side motivation add list and label text so motivations are authored from the left rail while the card only shows compact applied icons | `packages/ui-web/src/design-guidance.js`, `packages/ui-web/index.html`, `tests/ui-web/design-view.test.mjs`, `local-codex/Documentation.md` | Focused Design/UI regressions passed after deleting the card-local motivation add controls and simplifying the applied motivation display |
| 2026-03-14 | Moved Design/Preview/Run helper copy into the shared boxed status area and removed the duplicate inline helper text from those tab headers | `packages/ui-web/index.html`, `packages/ui-web/src/design-guidance.js`, `packages/ui-web/src/views/preview-view.js`, `packages/ui-web/src/views/simulation-view.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/preview-view.test.mjs`, `tests/ui-web/view-wiring.test.mjs`, `tests/ui-web/persona-tabs-layout.test.mjs`, `local-codex/Documentation.md` | Focused UI regressions and the full `ui-web` suite passed after making the boxed status area the only help/error surface on the primary workflow tabs |
| 2026-03-14 | Removed the empty Design header shell above the boxed status area by flattening the `Card Builder` title container | `packages/ui-web/index.html`, `local-codex/Documentation.md` | Static layout regression passed after replacing the unused `.stat` wrapper with a plain Design title container |
| 2026-03-15 | Compacted the Design workspace by shrinking toolbar controls, removing the empty budget overview box when unused, and dropping oversized minimum heights from the center canvas/card | `packages/ui-web/index.html`, `local-codex/Documentation.md` | Focused Design layout regressions and the full `ui-web` suite passed after removing vertical dead space from the Design tab |
| 2026-03-15 | Reweighted the Design layout so the center rail owns the screen: budget controls stack at the top, the boxed status follows immediately below, and the active card builder expands to fill the remaining center width instead of floating as a narrow centered card | `packages/ui-web/index.html`, `tests/ui-web/layout-hierarchy.test.mjs`, `local-codex/Documentation.md` | Focused layout regressions and the full `ui-web` suite passed after switching the center rail to content-stacked rows and widening the active card column |
| 2026-03-15 | Widened the Design left rail, paired opposed affinities side by side, and added an explicit `Choose 1` cue for exclusive motivation pairs while preserving the existing shared exclusivity enforcement | `packages/ui-web/index.html`, `packages/ui-web/src/design-guidance.js`, `tests/ui-web/design-view.test.mjs`, `tests/ui-web/layout-hierarchy.test.mjs`, `local-codex/Documentation.md` | Focused Design/layout regressions and the full `ui-web` suite passed after switching the left rail to paired affinity groups and explicit motivation exclusivity labels |

## 3) Decisions Log

| Date | Decision | Rationale | Impact |
| --- | --- | --- | --- |
| 2026-03-21 | Close this branch at the shared-hook boundary for `ipfs`, `blockchain`, and live runtime `llm` | Full productization of those capabilities is large enough to obscure the main parity/unification goal; keeping their hooks here preserves the single-rails architecture without forcing unrelated product scope into this branch | Branch-close work now focuses on parity-matrix normalization, optional-service boundaries, regression recording, and handoff; deeper capability work should continue on dedicated follow-on branches |
| 2026-03-01 | Use `local-codex/ui-cli-parity-matrix.md` as required planning artifact before implementation | Prevents hidden parity gaps and scope drift | Adds explicit inventory step (M1) |
| 2026-03-01 | Define parity as behavior + artifact equivalence, not UI visual equivalence | CLI cannot/should not replicate presentation-only behavior | Focuses work on functional and deterministic parity |
| 2026-03-01 | Classify Room parity as `partial` rather than `missing` | Runtime/build paths already support room behavior through BuildSpec/summary artifacts | Implementation focus shifts to CLI ergonomics, tests, and docs instead of core runtime changes |
| 2026-03-01 | Prioritize Room before Attacker/Defender in parity rollout | Room controls drive level shape/layout and budget spend; highest risk for CLI/UI drift | M2 scope starts with room authoring and budget-accounting verification |
| 2026-03-01 | Adopt “single command core” direction for UI/CLI unification | Prevents duplicated business logic and enables browser CLI execution | Requires extraction of Node-independent command kernel and UI integration changes |
| 2026-03-01 | Do not preserve legacy code paths for replaced UI/CLI behaviors | Parallel old/new execution paths increase drift and maintenance overhead | Legacy path deletion is now a required done criterion per milestone |
| 2026-03-01 | Approve `ui-only` classification only for presentation/interaction features that do not alter canonical artifacts | Keeps parity scope focused on domain behavior and artifact contract equivalence | Tab/layout and diagnostics visibility controls remain UI-only; no CLI parity required |
| 2026-03-07 | Add dedicated `room-plan` command for direct room authoring UX | Keeps existing `build` contract stable while enabling no-JSON room authoring parity in CLI | Room parity command now produces full build artifacts from repeatable room specs |
| 2026-03-07 | Add `room-plan` budget artifact inputs (`--budget` + `--price-list`) to enable E2E room cost accounting receipts | RP4 requires direct room-plan verification of `budget-receipt.json` cost lines/totals | Room authoring now supports deterministic budget receipt generation in the same command flow |
| 2026-03-07 | Add dedicated `attacker-plan` command for direct attacker authoring UX | Provides AP1 direct CLI parity without changing existing `build`/`llm-plan` contracts | Attacker parity command now produces full build artifacts from repeatable attacker specs |
| 2026-03-07 | Add dedicated `defender-plan` command for direct defender authoring UX | Provides DP1 direct CLI parity without changing existing `build`/`llm-plan` contracts | Defender parity command now produces full build artifacts from repeatable defender specs |
| 2026-03-07 | Extend attacker/defender plan specs with advanced `affinities` and `vitals` fields (plus attacker `setup-mode`) | Required to reach UI-equivalent actor tuning without hand-edited JSON | AP2/DP2 parity now supports expression/stacks and vitals through direct CLI UX |
| 2026-03-07 | Carry actor `affinities[]` into configurator actor inputs during buildspec assembly | Receipt parity requires spend proposal visibility into affinity expressions/stacks | Budget receipts now account for advanced attacker/defender affinity configuration from plan flows |
| 2026-03-08 | Use `run --actor` override in the Room recipe smoke run | Room-only plans have no actors, and run-time init requires at least one actor | AD1 room workflow remains end-to-end runnable without inventing a separate combined authoring command |
| 2026-03-08 | Centralize core command execution (`build`, `configurator`, `budget`, `llm-plan`) in `runtime/src/commands/kernel.js` with host-injected IO/env/adapters | Enables browser-host execution in later milestones and removes duplicated Node-specific command logic from CLI handlers | `ak-impl.mjs` now acts as an adapter shell for these commands; U2 can target worker host without re-implementing command policy |
| 2026-03-11 | Use a fetch-backed virtual filesystem for browser command execution | Keeps shared kernel file-oriented while allowing browser worker execution without Node/process coupling | Browser command host can execute `build` with inline spec JSON or fetched fixture/spec paths and return canonical artifacts to the UI |
| 2026-03-11 | Remove local UI build admission blocking from `wireDesignView` | Build gating must match artifact-first CLI behavior instead of diverging on a UI-only spend ledger decision | Minimal-budget or over-budget card sets now still flow through the same build command path as CLI-based artifact generation |
| 2026-03-11 | Browser host adapter wrappers must preserve command-scoped fixture `fetchFn` overrides | Shared-kernel commands such as `llm-plan` and adapter capture flows inject fixture-backed fetch functions at command scope | Browser-hosted `build` and `llm-plan` now honor fixture inputs exactly like Node CLI instead of accidentally falling back to host-global fetch behavior |
| 2026-03-11 | Define U6 equivalence at canonical artifact-tree level instead of raw byte-for-byte output | Generated artifact ids and timestamps differ across Node CLI and browser host even when behavior is equivalent | Integration tests normalize generated ids/timestamps while preserving structural/content parity checks for emitted artifacts |
| 2026-03-12 | Load `core-as.wasm` through the command-kernel host contract for `run`/`replay`/`inspect` | Keeps the simulation command path environment-agnostic and avoids Node-specific filesystem logic in shared runtime code | Node CLI and browser worker now execute the same simulation command logic with only host-level WASM/resource loading differences |
| 2026-03-12 | Reuse the existing host-injected solver adapter contract for `solve` instead of creating a second code path | `build` already proved the solver adapter boundary across Node and browser hosts; `solve` should use the same host service to avoid drift | `solve` now behaves as a thin shared-kernel command in both environments, and the browser host no longer diverges on `toRef` null-handling |
| 2026-03-15 | Do not close this branch on “feature parity only”; branch completion requires same-rails execution for the default workflow and an explicit minimum-install story | Direct authoring parity is done, but the branch goal is broader: UI and CLI should share command rails and avoid unnecessary dependency coupling | Remaining adapter-bound scope, dependency isolation, and baseline cleanup stay in active scope until branch close |
| 2026-03-15 | Start branch-close execution with the Design budget split / BuildSpec dispatch row rather than a lower-severity diagnostics slice | This row is high-severity, lives directly on the default workflow, and is already tied to a concrete failing baseline in `ak-configurator` | The next implementation work should resolve configurator layout intent first, then promote or further refine the parity row based on artifact-level evidence |
| 2026-03-15 | Rebaseline configurator fixtures to the current room-aware layout and resolved-affinity artifact contract | Runtime and browser-host tests already rely on room ids and richer resolved-affinity metadata; the old configurator fixtures were stale rather than authoritative | `ak-configurator` is now green again, the Design dispatch row can be treated as implemented, and the `TEST-CONFIG-1` blocker is resolved |
| 2026-03-15 | Keep `ipfs`, `blockchain`, and `llm` in shared-rails scope rather than classifying them as optional adapter-only demos | Product intent requires all three to participate in real game flows: IPFS stores canonical artifacts, blockchain mints canonical card configs, and LLM drives both authoring and structured runtime decisions | U4 now implements these capabilities on shared rails instead of deferring them out of parity scope |
| 2026-03-15 | Treat minimal-install as “no live service required for the baseline local workflow,” not “cut IPFS/blockchain/LLM from product scope” | The branch goal is low-friction local use, but the product still needs these capabilities. The right boundary is local-first live adapters plus a fixture-backed offline baseline | Default Design -> Preview -> Run remains service-free; live IPFS/blockchain/Ollama flows stay in scope and must use the same command rails when invoked |
| 2026-03-15 | Target local Ollama as the primary live LLM backend and require structured runtime decision contracts for actor or boss LLM use | A low-power/self-hosted device target rules out a cloud-only assumption, and runtime decisioning cannot safely depend on free-form string parsing | `llm-plan` and standalone/runtime `llm` work should converge on shared contracts, capture artifacts, and traceable structured responses |
| 2026-03-15 | Prefer solver-backed runtime reasoning for deterministic tactical decisions and reserve LLM runtime reasoning for structured advisory/live cases | The current runtime contracts already distinguish deterministic fulfillable effects from deferred/external ones. Z3/solver use fits execution-time tactical choice better than live LLM calls when replayability matters | Tactical next-move decisions such as choosing between two visible defenders should be modeled as solver-first reasoning. Live LLM decisions remain in scope, but must be structured, captured, and replay-aware rather than free-form execution-time text calls |
| 2026-03-15 | Standardize runtime reasoning on an embedded `runtime-decision-v1` payload carried through existing `SolverRequest` / `SolverResult` / `CapturedInputArtifact` artifacts | The repo already has the right transport primitives; adding another top-level decision artifact family would duplicate the runtime/effect/capture pipeline and increase drift risk | `B2-S4` implementation should extend the current solver/capture transport, normalize to `Action`, and record fulfillment in existing tick-frame/effect artifacts instead of inventing a second runtime reasoning rail |
| 2026-03-15 | Use the tick orchestrator as the first gameplay integration point for normalized solver decisions | The tick orchestrator already owns persona-effect collection and solver fulfillment, so it is the smallest correct place to turn a `SolverResult.model.decision` into an actionable runtime `Action` without adding a second dispatch path | Solver-selected candidate actions can now flow into `applyActionsToCore` on the existing rail; later slices should extend the same normalization model to captured LLM decisions and replay/inspect surfaces |
| 2026-03-15 | Lock today’s checkpoint at `B2-S4` and resume with `B2-S1` next | Runtime reasoning now has a coherent solver-first + captured/manual-live boundary; the larger branch risk has moved back to the shared-rails adapter work and minimal-install closure | Tomorrow’s work should start with IPFS artifact publish/load on shared rails, not by reopening the `B2-S4` transport design unless branch-close later requires automatic solver-to-LLM fallback |
| 2026-03-14 | Remove the legacy Game Board/runtime UI instead of preserving it beside the browser-hosted build flow | The active product workflow is Design + Diagnostics, and keeping an unreachable playback/runtime branch violates the no-legacy policy for replaced UI scope | `index.html`, `main.js`, and the UI test suite now only cover the active interface surface; stale runtime/playback modules and tests were deleted rather than left dormant |
| 2026-03-14 | Reintroduce preview as a minimal artifact-driven UI loop, not as a standalone runtime subsystem | The product still needs a fast visual validation loop from Design, but that loop should render canonical build artifacts instead of reviving the removed legacy runtime controls | The active interface is now Design + Preview + Diagnostics; Preview reads the current bundle and falls back to layout-only rendering when no actors exist |
| 2026-03-14 | Preserve single-element design iteration in the active Design -> Preview loop | The Design tab must support authoring and validating one room or one attacker in isolation before the user expands or edits the card set | `Build And Load Game` is now regression-tested for room-only and attacker-only card sets, and Preview explicitly supports both layout-only and actor-backed artifact rendering |
| 2026-03-14 | Restore Game Board as the primary post-build landing surface while keeping Preview as a secondary artifact view | The product still needs interactive board controls and selected-card inspection after build, but Preview remains useful for quick artifact-only validation | `Build And Load Game` now opens Game Board again, Preview stays available as a separate tab, both surfaces are loaded from the same canonical bundle artifacts via `main.js`, and room-only bundles now reset Game Board instead of leaking prior runtime state |
| 2026-03-14 | Do not reintroduce a separate Diagnostics runtime-launch button | Bundle Review already emits bundle-load events, and routing Game Board/Preview updates centrally avoids UI drift and duplicate controls | Diagnostics remains an artifact inspection/edit surface while `main.js` handles distributing bundle loads to Game Board and Preview |
| 2026-03-14 | Remove the separate Selected Actor View and treat the playing surface plus right-rail inspector as the single runtime interaction surface | The bottom viewport duplicated board state, selection context, and action targeting without adding distinct runtime behavior | Game Board now has one authoritative interaction surface, the UI is smaller, and movement/cast commands resolve from the currently selected inspector entity inside `simulation-view` |
| 2026-03-14 | Make Preview the required staging step between Design and Game Board | The workflow should support repeated Design/Preview iterations before the user launches the interactive runtime surface | Tab order is now Design -> Preview -> Game Board, Design publishes BuildSpec updates, Preview refreshes the current bundle through the browser-hosted build path, and the `Build And Load Game` control now lives on the Preview surface |
| 2026-03-14 | Keep Design messaging localized to the editor rail and hide it until there is a real status to show | The old top-of-panel status line read like a blank container and the helper sentence duplicated affordances already visible in the UI | Drop errors such as `missing_type` now appear in a boxed status area directly above the card display, and the blank/instructional chrome has been removed from the Design surface |
| 2026-03-14 | Auto-generate should top up the current per-type allocation instead of replacing the existing shelves or inventing synthetic spend-only placeholders | The user needs a fast manual-testing shortcut without losing the current design state, and the generated cards should still be real card configurations visible in the editor/receipts | The Design tab now appends deterministic room/attacker/defender cards using the remaining allocation, preserves existing shelved cards, and leaves only the unavoidable remainder in each bucket |
| 2026-03-14 | Treat Preview as permissive for partial designs, but require the authored card set to contain at least one room, attacker, and defender before opening Run | Preview is still useful for room-only or attacker-only artifact inspection, but the interactive run surface should not launch from incomplete authored sets | `Build And Load Game` now validates `bundle.spec.plan.hints.cardSet` after the Preview refresh completes and returns a user-visible error instead of opening Run when any required type is missing |
| 2026-03-14 | Use one boxed status component as the standard central-surface feedback template for Design, Preview, and Run | The interface had drifted into mixed feedback patterns (`Design` box, `Preview` plain line, `Run` pill in controls), which made errors feel inconsistent and harder to scan | All three tabs now render status/error feedback in the same location and style above the main surface, while Preview/Run also set explicit `info`/`error` levels in view code |
| 2026-03-14 | Treat motivations as stackable across orthogonal axes, but reject combinations inside the same exclusive family | The product request is to prevent contradictory authoring such as `attacking` + `defending` without collapsing motivations to a single-choice system | Shared normalization now enforces exclusive families for combat (`attacking`/`defending`), movement (`stationary`/`exploring`/`patrolling`), planning (`random`/`strategy_focused`), and response (`reflexive`/`goal_oriented`) |
| 2026-03-14 | Keep the direct CLI attacker/defender authoring UX singular for `motivation` and reject duplicate declarations explicitly | Direct CLI authoring already models one `motivation=` field; silently overwriting repeated entries would hide contradictory input instead of enforcing it | `attacker-plan` and `defender-plan` now fail fast when `motivation` is repeated in the same spec, and the README documents that constraint |
| 2026-03-14 | Use the boxed status area as the default help surface on Design, Preview, and Run, and remove duplicate inline helper copy from those panels | The small helper lines consumed vertical space and duplicated guidance that now fits naturally into the shared status template | The three primary tabs now ship with short boxed help text by default, while errors and transient workflow messages still overwrite the same element in place |
| 2026-03-15 | Prefer content-driven height on the Design tab over fixed-height editor chrome | The Design view was forcing unnecessary scrolling on large screens because the toolbar and card canvas reserved space even when they had little or no content | The budget controls are tighter, the cleared budget overview no longer renders as an empty box, and the editor canvas/card now size to their actual content instead of a desktop-only minimum height |
| 2026-03-15 | Make the Design center rail the primary visual focus instead of a narrow centered card inside a stretched column | The screenshot review showed the active card being visually de-emphasized by stretched grid rows and a fixed-width builder that left most of the center rail empty | The center rail now uses `align-content: start`, the builder sits directly under the budget/status controls, and the active card expands across the center column on desktop and wide-screen layouts |
| 2026-03-15 | Represent opposed affinities and exclusive motivations explicitly in the left rail instead of leaving those relationships implicit | The user should be able to understand natural opposition (`fire` vs `water`) and mutual exclusion (`attacking` vs `defending`) from the UI without trial-and-error | The left rail now renders affinity chips in opposed pairs and labels multi-option motivation groups with `Choose 1`, while runtime/CLI normalization continues to enforce the exclusivity contract underneath |

## 4) Validation Log

| Date | Command | Result | Notes |
| --- | --- | --- | --- |
| 2026-04-08 | `node --test tests/ui-web/view-wiring.test.mjs` | Passed | Guardrail sweep after wiring BuildSpec normalization and design hydration into the UI shell; missing-DOM tolerance still holds |
| 2026-04-08 | `node --test tests/ui-web/actor-inspector.test.mjs` | Passed | Inspector still renders template cards after switching to merged plan/configurator card-set extraction |
| 2026-04-08 | `node --test tests/ui-web/preview-view.test.mjs tests/ui-web/bundle-integration.test.mjs` | Passed | Preview launch validation now accepts split authored card sets, and Bundle Review normalizes agent-authored singleton fields on load |
| 2026-04-08 | `node --test --test-name-pattern='wireDesignView hydrates editor cards from an agent-authored build spec' tests/ui-web/design-view.test.mjs` | Passed | Targeted design hydration coverage for agent-authored bundle/spec ingestion; full `design-view.test.mjs` was not rerun because this workspace currently has unrelated baseline failures in older auto-generate/allocation cases |
| 2026-04-08 | `node --test tests/integration/ui-cli-equivalence.test.js` | Passed | Browser command host parity still holds, and the UI path now has explicit coverage for `normalize_build_spec` agent-authoring normalization |
| 2026-03-15 | `node --test tests/runtime/runtime-decision-contract.test.js tests/personas/tick-orchestrator.test.js tests/runtime/runner-effects.test.js tests/personas/orchestrator-llm-capture.test.js` | Passed | `B2-S4` targeted validation: shared `runtime-decision-v1` helper, solver-first action selection in tick orchestrator, replay-safe captured LLM decision resolution, and no regression in runner/capture routing |
| 2026-03-15 | `node --test tests/personas/actor-runtime-decision.test.js tests/personas/actor-persona-movement.test.js tests/personas/actor-persona-filter.test.js tests/runtime/runtime-decision-contract.test.js tests/runtime/run-helpers-runtime-decision.test.js tests/runtime/command-kernel-inspect-runtime-decision.test.js tests/personas/tick-orchestrator.test.js` | Passed | `B2-S4` gameplay + summary slice: actor solver-request emission, provider policy, runtime decision summary helpers, inspect surfacing, and no regression in existing movement/filter/tick-orchestrator behavior |
| 2026-03-15 | `node --test tests/personas/actor-persona-phase.test.js tests/personas/actor-budget-gating.test.js tests/personas/allocator-actor-behavior.test.js tests/runtime/runner-effects.test.js tests/runtime/tick-frames.test.js tests/adapters-cli/ak.test.js` | Passed | Regression ring after actor/runtime summary changes: actor phases/budgeting, allocator interplay, runner effect routing, tick-frame capture, and CLI `run`/`replay`/`inspect` outputs remained green |
| 2026-03-15 | `node --test tests/personas/actor-runtime-decision.test.js tests/personas/tick-orchestrator.test.js tests/runtime/runtime-decision-contract.test.js tests/runtime/run-helpers-runtime-decision.test.js tests/runtime/command-kernel-inspect-runtime-decision.test.js` | Passed | Manual live-Ollama runtime slice: explicit manual-mode provider policy, actor emission on `solver_request`, tick-orchestrator live LLM fulfillment with capture artifacts, and runtime decision summary helpers |
| 2026-03-15 | `node --test tests/personas/actor-persona-phase.test.js tests/personas/actor-persona-movement.test.js tests/personas/actor-persona-filter.test.js tests/personas/actor-budget-gating.test.js tests/personas/allocator-actor-behavior.test.js tests/runtime/runner-effects.test.js tests/runtime/tick-frames.test.js tests/adapters-cli/ak.test.js` | Passed | Regression ring after live manual-mode LLM addition: actor baseline behaviors, allocator interplay, runner effect routing, tick-frame capture, and CLI `run`/`replay`/`inspect` remained green |
| 2026-03-15 | `node --test tests/personas/director-solver.test.js tests/personas/configurator-solver.test.js tests/personas/allocator-solver.test.js tests/contracts/captured-input-artifact.test.js` | Passed | Secondary regression sweep confirms existing solver-emitting personas and capture artifact contracts remain green after the runtime-decision helper and tick-orchestrator wiring |
| 2026-03-01 | N/A | N/A | Planning/setup phase only; implementation validations pending |
| 2026-03-01 | `rg -n "DEFAULT_ROOM|roomSize|affinit|count" ...` across UI/runtime/CLI | Completed | Confirmed room defaults (`dark`, `emit`, `stacks=2`) in UI/runtime and no direct CLI room flags |
| 2026-03-01 | `rg -n "data-tab|id=" packages/ui-web/index.html` | Completed | Enumerated full UI feature surface for parity inventory (Design/Game Board/Diagnostics + controls) |
| 2026-03-01 | `rg -n "case \"|function .*Command" packages/adapters-cli/src/cli/ak-impl.mjs` | Completed | Confirmed current CLI command surface for parity mapping (`build`, `schemas`, `solve`, `run`, `configurator`, `budget`, `replay`, `inspect`, `ipfs`, `blockchain`, `llm`, `llm-plan`) |
| 2026-03-07 | `node --test tests/adapters-cli/ak-room-plan.test.js` | Passed | New room authoring CLI tests (direct authoring, defaults, multi-room, validation errors) |
| 2026-03-07 | `node --test tests/adapters-cli/ak-room-plan.test.js tests/adapters-cli/ak-build.test.js tests/adapters-cli/ak-errors.test.js` | Passed | Regression check for new command plus existing build/error paths |
| 2026-03-07 | `node --test tests/adapters-cli/ak-build.test.js tests/adapters-cli/ak-llm-plan.test.js tests/adapters-cli/ak-errors.test.js` | Failed (2 tests) | Existing `ak-llm-plan` budget-loop expectations failed in this workspace (`spentTokens` and `remainingBudgetTokens` assertions); unrelated to `room-plan` changes |
| 2026-03-07 | `node --test tests/adapters-cli/ak-room-plan.test.js` | Passed | Includes RP4 coverage for `budget-receipt.json` layout + room-affinity-driven trap spend assertions and `--budget`/`--price-list` arg validation |
| 2026-03-07 | `node --test tests/adapters-cli/ak-room-plan.test.js tests/adapters-cli/ak-build.test.js tests/adapters-cli/ak-errors.test.js` | Passed | Regression sweep after adding room-plan budget flags and receipt assertions |
| 2026-03-07 | `node --test tests/runtime/design-spend-ledger.test.js` | Passed | Runtime room cost-model guardrail remains green after RP4 changes |
| 2026-03-07 | `node --test tests/adapters-cli/ak-attacker-plan.test.js` | Passed | New AP1 attacker authoring tests (direct authoring, defaults, multi-attacker, validation errors) |
| 2026-03-07 | `node --test tests/adapters-cli/ak-attacker-plan.test.js tests/adapters-cli/ak-room-plan.test.js tests/adapters-cli/ak-build.test.js tests/adapters-cli/ak-errors.test.js` | Passed | Regression sweep after adding `attacker-plan` command and CLI docs wiring |
| 2026-03-07 | `node --test tests/runtime/summary-selections.test.js` | Passed | Runtime summary normalization/selection guardrail remains green after AP1 command additions |
| 2026-03-07 | `node --test tests/adapters-cli/ak-defender-plan.test.js` | Passed | New DP1 defender authoring tests (direct authoring, defaults, multi-defender, validation errors) |
| 2026-03-07 | `node --test tests/adapters-cli/ak-defender-plan.test.js tests/adapters-cli/ak-attacker-plan.test.js tests/adapters-cli/ak-room-plan.test.js tests/adapters-cli/ak-build.test.js tests/adapters-cli/ak-errors.test.js` | Passed | Regression sweep after adding `defender-plan` command and CLI docs wiring |
| 2026-03-07 | `node --test tests/runtime/summary-selections.test.js` | Passed | Runtime summary normalization/selection guardrail remains green after DP1 command additions |
| 2026-03-07 | `node --test tests/adapters-cli/ak-attacker-plan.test.js` | Passed | AP2 advanced attacker tests (affinities+stacks, vitals, setup-mode, receipt assertions) |
| 2026-03-07 | `node --test tests/adapters-cli/ak-defender-plan.test.js` | Passed | DP2 advanced defender tests (affinities+stacks, vitals, receipt assertions) |
| 2026-03-07 | `node --test tests/adapters-cli/ak-attacker-plan.test.js tests/adapters-cli/ak-defender-plan.test.js tests/adapters-cli/ak-room-plan.test.js tests/adapters-cli/ak-build.test.js tests/adapters-cli/ak-errors.test.js` | Passed | Regression sweep after AP2/DP2 parser + runtime spend propagation updates |
| 2026-03-07 | `node --test tests/runtime/summary-selections.test.js` | Passed | Runtime summary normalization remains green after AP2/DP2 changes |
| 2026-03-08 | `node packages/adapters-cli/src/cli/ak.mjs room-plan --room "size=small;count=2;affinities=dark:emit:2,fire:push:1" --room "size=large;count=1;affinities=water:pull:1" --budget tests/fixtures/artifacts/budget-artifact-v1-basic.json --price-list tests/fixtures/artifacts/price-list-artifact-v1-basic.json --run-id run_room_parity_recipe --created-at 2026-03-08T00:00:00Z --out-dir artifacts/parity-recipes/room` | Passed | AD1 room parity recipe authoring command |
| 2026-03-08 | `node packages/adapters-cli/src/cli/ak.mjs run --sim-config artifacts/parity-recipes/room/sim-config.json --initial-state artifacts/parity-recipes/room/initial-state.json --actor room_probe,1,1,motivated --ticks 0 --run-id run_room_parity_recipe_playback --created-at 2026-03-08T00:00:00Z --out-dir artifacts/parity-recipes/room-run` | Passed | AD1 room parity recipe smoke run (actor override) |
| 2026-03-08 | `node packages/adapters-cli/src/cli/ak.mjs attacker-plan --attacker "count=1;affinity=fire;motivation=attacking;setup-mode=user;affinities=fire:push:3,wind:emit:2;vitals=health:12:12:1,mana:7:7:2,stamina:6:6:1,durability:5:5:0" --budget tests/fixtures/artifacts/budget-artifact-v1-basic.json --price-list tests/fixtures/artifacts/price-list-artifact-v1-basic.json --run-id run_attacker_parity_recipe --created-at 2026-03-08T00:00:00Z --out-dir artifacts/parity-recipes/attacker` | Passed | AD1 attacker parity recipe authoring command |
| 2026-03-08 | `node packages/adapters-cli/src/cli/ak.mjs run --sim-config artifacts/parity-recipes/attacker/sim-config.json --initial-state artifacts/parity-recipes/attacker/initial-state.json --ticks 0 --run-id run_attacker_parity_recipe_playback --created-at 2026-03-08T00:00:00Z --out-dir artifacts/parity-recipes/attacker-run` | Passed | AD1 attacker parity recipe playback |
| 2026-03-08 | `node packages/adapters-cli/src/cli/ak.mjs defender-plan --defender "count=1;affinity=dark;motivation=defending;affinities=dark:emit:4,earth:pull:1;vitals=health:15:15:0,mana:3:3:1,stamina:4:4:1,durability:8:8:0" --budget tests/fixtures/artifacts/budget-artifact-v1-basic.json --price-list tests/fixtures/artifacts/price-list-artifact-v1-basic.json --run-id run_defender_parity_recipe --created-at 2026-03-08T00:00:00Z --out-dir artifacts/parity-recipes/defender` | Passed | AD1 defender parity recipe authoring command |
| 2026-03-08 | `node packages/adapters-cli/src/cli/ak.mjs run --sim-config artifacts/parity-recipes/defender/sim-config.json --initial-state artifacts/parity-recipes/defender/initial-state.json --ticks 0 --run-id run_defender_parity_recipe_playback --created-at 2026-03-08T00:00:00Z --out-dir artifacts/parity-recipes/defender-run` | Passed | AD1 defender parity recipe playback |
| 2026-03-08 | `node --test tests/adapters-cli/ak-build.test.js` | Passed | U1 regression check: build command still green after kernel extraction |
| 2026-03-08 | `node --test tests/adapters-cli/ak-budget.test.js` | Passed | U1 regression check: budget command still green after kernel extraction |
| 2026-03-08 | `node --test tests/adapters-cli/ak-room-plan.test.js` | Passed | U1 safety regression check for direct room authoring command |
| 2026-03-08 | `node --test tests/runtime/command-kernel-budget.test.js` | Passed | New runtime-level kernel test validates host-injected IO flow without Node path/process coupling in command logic |
| 2026-03-08 | `node --test tests/adapters-cli/ak-configurator.test.js` | Failed (1 test) | Existing fixture expectation drift in `cli configurator builds sim config and initial state artifacts` (layout/spawn/exit shape mismatch) observed during U1 validation |
| 2026-03-15 | `node --test tests/adapters-cli/ak-configurator.test.js` | Failed (1 test) | Reconfirmed `B1-S1` blocker: expected configurator fixture no longer matches actual layout output (`spawn`, `exit`, `entryRoomId`, `exitRoomId`, and room `id` fields differ) |
| 2026-03-15 | `node --test tests/adapters-cli/ak-configurator.test.js tests/personas/configurator-artifact-fixtures.test.js tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js tests/ui-web/design-view.test.mjs` | Passed | `B1-S1` validation sweep after rebaselining configurator fixtures and reaffirming the shared-rails Design dispatch path |
| 2026-03-08 | `node --test tests/adapters-cli/ak-llm-plan.test.js` | Failed (2 tests) | Existing baseline `TEST-LLM-1`: budget-loop telemetry expectations (`spentTokens`, `remainingBudgetTokens`) still mismatched |
| 2026-03-11 | `node --test tests/adapters-web/cli-worker.test.js` | Passed | Browser command host regression: in-process build execution plus worker success/timeout/cancel semantics |
| 2026-03-11 | `node --test tests/ui-web/*.test.mjs` | Passed | UI regression sweep after replacing bridge-backed build flow with browser command host wiring |
| 2026-03-11 | `node --test tests/adapters-cli/ak-build.test.js tests/runtime/command-kernel-budget.test.js` | Passed | Shared build kernel + Node adapter regressions remained green after browser host addition |
| 2026-03-11 | `node --test tests/adapters-web/*.test.js` | Failed (1 test) | Existing unrelated `tests/adapters-web/adapter-modules.test.js` level-builder expectation mismatch (`walkableTiles` 120 vs 150) observed during broader web adapter sweep |
| 2026-03-11 | `node --test tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Shared browser-host command coverage + U6 equivalence suite now green for `build`, `configurator`, `budget`, and `llm-plan` |
| 2026-03-11 | `node --test tests/ui-web/*.test.mjs` | Passed | UI web regression rerun after fixing browser-host fixture override behavior in `cli-worker/shared.js` |
| 2026-03-12 | `node --test tests/adapters-cli/ak.test.js tests/adapters-cli/ak-affinity-summary.test.js tests/adapters-cli/ak-actor-flags.test.js tests/adapters-cli/ak-actor-determinism.test.js tests/adapters-cli/ak-errors.test.js` | Passed | Targeted CLI regression sweep after extracting shared `run`/`replay`/`inspect` command logic |
| 2026-03-12 | `node --test tests/adapters-web/cli-worker.test.js` | Passed | Browser worker + in-process host now cover `run`, `replay`, and `inspect` in addition to build/planning commands |
| 2026-03-12 | `node --test tests/integration/ui-cli-equivalence.test.js` | Passed | Node-vs-browser canonical artifact equivalence now includes `run`, `replay`, and `inspect` outputs |
| 2026-03-12 | `node --test tests/adapters-cli/ak.test.js tests/adapters-cli/ak-golden.test.js tests/adapters-cli/ak-affinity-summary.test.js tests/adapters-cli/ak-actor-flags.test.js tests/adapters-cli/ak-actor-determinism.test.js tests/adapters-cli/ak-errors.test.js tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Combined CLI/browser/equivalence regression sweep after the runtime command-kernel slice landed |
| 2026-03-12 | `node --test tests/ui-web/*.test.mjs` | Passed | UI web regression remained green after adding browser-host support for runtime artifact commands |
| 2026-03-12 | `node --test tests/runtime/command-kernel-solve.test.js` | Passed | New runtime-level kernel test validates `solve` artifact emission through injected host IO and solver adapter services |
| 2026-03-12 | `node --test tests/adapters-cli/ak.test.js tests/adapters-cli/ak-golden.test.js tests/adapters-cli/solver.test.js` | Passed | CLI `solve` behavior stayed stable after the shared-kernel extraction |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | UI regression sweep after removing the legacy Game Board/runtime tab, actor inspector, playback controls, and placeholder diagnostics sections |
| 2026-03-14 | `node --test tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Browser-host and Node-vs-browser equivalence coverage remained green after the UI surface cleanup |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | UI regression sweep after restoring the Preview tab and `Build And Load Game` loop from canonical bundle artifacts |
| 2026-03-14 | `node --test tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Browser-host and Node-vs-browser equivalence coverage remained green after the preview-loop restoration |
| 2026-03-14 | `node --test tests/ui-web/design-view.test.mjs tests/ui-web/preview-view.test.mjs tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/view-wiring.test.mjs tests/ui-web/layout-hierarchy.test.mjs` | Passed | Targeted UI workflow sweep including explicit single-room and single-attacker `Build And Load Game` coverage |
| 2026-03-14 | `node --input-type=module <<'EOF' ... buildSpecFromSummaryFlow + ak.mjs build ... EOF` | Passed | Manual artifact smoke check confirmed both room-only and attacker-only design summaries emit `sim-config.json` and `initial-state.json` through the canonical build pipeline |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after restoring Game Board, runtime controls, and the right-rail inspector while keeping Preview intact |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after restoring Game Board on top of the browser-hosted bundle flow |
| 2026-03-14 | `node --test tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Browser-host build/runtime/equivalence coverage remained green after the Game Board restoration |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after removing the redundant Selected Actor View and rebinding runtime actions to the selected inspector entity |
| 2026-03-14 | `node --test tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Browser-host build/runtime/equivalence coverage remained green after simplifying Game Board to a single interaction surface |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/view-wiring.test.mjs tests/ui-web/design-view.test.mjs tests/ui-web/preview-view.test.mjs` | Passed | Focused regression sweep after moving `Build And Load Game` to Preview and making Design editor-only |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after reordering the workflow to Design -> Preview -> Game Board and auto-refreshing Preview from the current design bundle |
| 2026-03-14 | `node --test tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Browser-host build/runtime/equivalence coverage remained green after Preview became the staging surface for game launch |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/design-view.test.mjs tests/ui-web/view-wiring.test.mjs` | Passed | Focused regression sweep after moving Design status/error messaging into the center rail and removing the obsolete helper copy |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after the Design status-box relocation and copy cleanup |
| 2026-03-14 | `node --test tests/ui-web/design-view.test.mjs tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/view-wiring.test.mjs` | Passed | Focused regression sweep after adding the Card Builder `Auto-generate` control, budget-aware auto-fill logic, and the Design view wiring |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after adding deterministic room/attacker/defender auto-generation for manual testing workflows |
| 2026-03-14 | `node --test tests/ui-web/preview-view.test.mjs tests/ui-web/view-wiring.test.mjs tests/ui-web/design-view.test.mjs` | Passed | Focused regression sweep after adding the Preview launch guard for incomplete room/attacker/defender authored sets |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after allowing partial previews but blocking Run launch when the authored card set is incomplete |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/preview-view.test.mjs tests/ui-web/view-wiring.test.mjs tests/ui-web/simulation-view.test.mjs` | Passed | Focused regression sweep after standardizing the central status/error box across Design, Preview, and Run |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after replacing Preview/Run ad hoc status treatments with the shared boxed status component |
| 2026-03-14 | `node --test tests/runtime/motivation-loadouts.test.js tests/runtime/summary-selections.test.js tests/adapters-cli/ak-attacker-plan.test.js tests/adapters-cli/ak-defender-plan.test.js` | Passed | Shared runtime normalization and direct CLI parser coverage for contradictory/duplicate motivation enforcement |
| 2026-03-14 | `node --test tests/ui-web/design-guidance-affinity-sync.test.mjs tests/ui-web/design-view.test.mjs` | Passed | Focused Design authoring regression sweep for paired motivation rendering, disabled contradictory options, and conflict handling |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after wiring paired motivation groups into the designer and inspector normalization paths |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/actor-inspector.test.mjs` | Passed | Run inspector layout and right-rail grouping remained green after capping each list to a four-row scroll viewport |
| 2026-03-14 | `node --test tests/ui-web/simulation-view.test.mjs` | Passed | Run surface smoke coverage remained green after the inspector scroll styling change |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/view-wiring.test.mjs` | Passed | Run layout and DOM wiring remained green after removing the redundant Game Inspector header status line |
| 2026-03-14 | `node --test tests/ui-web/actor-inspector.test.mjs tests/ui-web/simulation-view.test.mjs` | Passed | Inspector selection/detail behavior remained green after the Run-side header cleanup |
| 2026-03-14 | `node --test tests/ui-web/design-view.test.mjs tests/ui-web/design-guidance-affinity-sync.test.mjs` | Passed | Design card motivation UI remained green after removing the card-local add list and text labels in favor of left-rail-driven authoring |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/view-wiring.test.mjs` | Passed | Layout and view wiring remained green after the card-side motivation UI cleanup |
| 2026-03-14 | `node --test tests/ui-web/design-view.test.mjs tests/ui-web/preview-view.test.mjs tests/ui-web/view-wiring.test.mjs tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/simulation-view.test.mjs` | Passed | Focused UI regression sweep after moving Design/Preview/Run helper copy into the shared boxed status area and removing the duplicate inline helper lines |
| 2026-03-14 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after making the boxed status area the default help/error surface on the primary workflow tabs |
| 2026-03-14 | `node --test tests/ui-web/persona-tabs-layout.test.mjs` | Passed | Static Design/Preview/Run layout remained green after removing the empty Design header shell above the boxed status area |
| 2026-03-15 | `node --test tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/design-view.test.mjs` | Passed | Focused Design layout and behavior regressions remained green after compacting the toolbar/canvas and hiding the empty budget overview box |
| 2026-03-15 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after removing fixed-height dead space from the Design tab |
| 2026-03-15 | `node --test tests/ui-web/layout-hierarchy.test.mjs tests/ui-web/persona-tabs-layout.test.mjs tests/ui-web/design-view.test.mjs` | Passed | Static layout and Design behavior regressions remained green after widening the center rail and pinning the budget/status/card stack to the top of the Design workspace |
| 2026-03-15 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after making the card builder the dominant element in the Design center rail |
| 2026-03-15 | `node --test tests/ui-web/design-view.test.mjs tests/ui-web/layout-hierarchy.test.mjs tests/ui-web/persona-tabs-layout.test.mjs` | Passed | Focused Design and layout regressions remained green after widening the left rail, pairing opposed affinities, and adding explicit exclusivity cues to motivation pairs |
| 2026-03-15 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after the left-rail pairing and exclusivity-label update |
| 2026-03-12 | `node --test tests/adapters-web/cli-worker.test.js` | Passed | Browser host now exposes `solve` alongside the other shared-kernel commands |
| 2026-03-12 | `node --test tests/integration/ui-cli-equivalence.test.js` | Passed | Node-vs-browser canonical artifact equivalence now includes `solve` outputs |
| 2026-03-12 | `node --test tests/runtime/command-kernel-solve.test.js tests/adapters-cli/ak.test.js tests/adapters-cli/ak-golden.test.js tests/adapters-cli/solver.test.js tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Consolidated solve-focused kernel/CLI/browser/equivalence regression sweep |
| 2026-03-12 | `node --test tests/ui-web/*.test.mjs` | Passed | UI web regression remained green after expanding the shared browser-host command surface to include `solve` |
| 2026-03-21 | `node --test tests/adapters-cli/ak-llm-plan.test.js` | Passed | Rebased budget-loop telemetry expectations to current hallway-cost semantics (`deprecated_hallway_tiles_ignored`) |
| 2026-03-21 | `node --test tests/adapters-web/adapter-modules.test.js` | Passed | Rebased level-builder walkable tile expectations to the current floor-tile-driven target |
| 2026-03-21 | `node --test tests/adapters-cli/*.test.js` | Passed | Full adapters-cli regression sweep green after rebaseline |
| 2026-03-21 | `node --test tests/adapters-web/*.test.js` | Passed | Full adapters-web regression sweep green after rebaseline |
| 2026-03-21 | `node --test tests/ui-web/budget-panels.test.mjs tests/ui-web/diagnostics-view.test.mjs` | Passed | New diagnostics budget-panel artifact wiring coverage |
| 2026-03-21 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression sweep remained green after budget-panel live wiring |
| 2026-03-21 | `node --test tests/integration/*.test.js` | Passed (1 skipped live test) | Integration/equivalence sweep green after refreshing e2e prompt fixture drift |
| 2026-03-21 | `node --test tests/adapters-cli/ak-adapter-commands.test.js tests/adapters-web/cli-worker.test.js tests/ui-web/adapter-playground.test.mjs` | Passed | Shared-rails standalone `ipfs`/`blockchain`/`llm` browser-host + UI adapter-panel coverage |
| 2026-03-21 | `node --test tests/adapters-cli/ak-adapter-commands.test.js` | Passed | Added `ipfs-load` CLI coverage for canonical artifact reload from fixture-mapped CID content |
| 2026-03-21 | `node --test tests/adapters-web/cli-worker.test.js` | Passed | Added `ipfs_load` browser-host coverage including bundle/manifest restoration payloads |
| 2026-03-21 | `node --test tests/adapters-cli/*.test.js` | Passed | adapters-cli sweep remained green after moving standalone adapter commands onto kernel rails |
| 2026-03-21 | `node --test tests/adapters-web/*.test.js` | Passed | adapters-web sweep remained green after exposing `ipfs`/`blockchain`/`llm` through cli-worker host |
| 2026-03-21 | `node --test tests/ui-web/*.test.mjs` | Passed | UI web sweep remained green after routing adapter panel calls onto command-host methods |
| 2026-03-21 | `node --test tests/integration/*.test.js` | Passed (1 skipped live test) | Integration/equivalence sweep remained green after shared-rails adapter-command extraction |
| 2026-03-21 | `node --test tests/runtime/configurator-startup.test.js` | Passed | Rebaselined configurator startup expectation to current fixture exit coordinate (`1,3`) |
| 2026-03-21 | `node --test tests/adapters-cli/ak-adapter-commands.test.js tests/adapters-web/cli-worker.test.js tests/ui-web/adapter-playground.test.mjs` | Passed | Added `ipfs-publish` shared-rails coverage in CLI/browser-host/UI adapter panel |
| 2026-03-21 | `node --test tests/adapters-cli/*.test.js` | Passed | adapters-cli sweep remained green after adding IPFS publish adapter+command support |
| 2026-03-21 | `node --test tests/adapters-web/*.test.js` | Passed | adapters-web sweep remained green after adding IPFS publish adapter+host support |
| 2026-03-21 | `node --test tests/integration/ui-cli-equivalence.test.js` | Passed | Node-vs-browser equivalence now covers `ipfs-publish` and `ipfs-load` lifecycle outputs |
| 2026-03-21 | `node --test tests/adapters-cli/ak-adapter-commands.test.js tests/adapters-cli/ak-errors.test.js tests/adapters-cli/adapter-modules.test.js tests/adapters-web/adapter-modules.test.js tests/adapters-web/cli-worker.test.js tests/ui-web/design-view.test.mjs tests/integration/ui-cli-equivalence.test.js` | Passed | Added blockchain mint/load contract coverage across CLI/browser host/adapters/UI design workflow and equivalence |
| 2026-03-21 | `node --test tests/adapters-cli/*.test.js` | Passed | adapters-cli sweep remained green after adding `blockchain-mint`/`blockchain-load` commands |
| 2026-03-21 | `node --test tests/adapters-web/*.test.js` | Passed | adapters-web sweep remained green after adding blockchain mint/load adapter+host support |
| 2026-03-21 | `node --test tests/ui-web/*.test.mjs` | Passed | UI web sweep remained green after wiring Design `Mint` to blockchain rails and adding load-minted support |
| 2026-03-21 | `node --test tests/integration/*.test.js` | Passed (1 skipped live test) | Integration/equivalence sweep remained green after blockchain mint/load parity additions |
| 2026-03-21 | `node --test tests/ui-web/ollama-panel.test.mjs` | Passed | Ollama prompt panel now prefers shared `commandHost.llm` execution in fixture mode while preserving BuildSpec normalization/validation flow |
| 2026-03-21 | `node --test tests/integration/ui-cli-equivalence.test.js` | Passed | Node-vs-browser equivalence now includes standalone `llm` command artifacts in addition to existing `llm-plan` coverage |
| 2026-03-21 | `node --test tests/ui-web/*.test.mjs` | Passed | Full UI web regression remained green after moving Ollama prompt execution onto shared command rails |
| 2026-03-21 | `node --test tests/adapters-web/*.test.js` | Passed | adapters-web regression remained green after the standalone `llm` equivalence extension |
| 2026-03-21 | `node --test tests/runtime/run-helpers-runtime-decision.test.js tests/runtime/command-kernel-inspect-runtime-decision.test.js tests/adapters-cli/ak.test.js tests/adapters-web/cli-worker.test.js tests/integration/ui-cli-equivalence.test.js` | Passed | Runtime-decision capture lifecycle surfacing validation (`runtime-decision-captures.json` + inspect/replay capture telemetry) |
| 2026-03-21 | `node --test tests/runtime/runtime-decision-contract.test.js tests/personas/tick-orchestrator.test.js` | Passed | Runtime fallback policy lock validation (`allowLlmFallback=false` default, no implicit solver->LLM fallback) |
| 2026-03-21 | `node --test tests/runtime/*.test.js` | Passed | Runtime baseline sweep now passes after rebaselining `tests/runtime/configurator-startup.test.js` to the current fixture exit coordinate |
| 2026-03-21 | `node --test tests/adapters-cli/*.test.js` | Passed | Full adapters-cli regression remained green after runtime decision capture artifact emission |
| 2026-03-21 | `node --test tests/adapters-web/*.test.js` | Passed | Full adapters-web regression remained green after exposing run-time capture artifacts through browser host output |

## 5) Open Issues / Blockers

| ID | Issue | Severity | Owner | Next Step |
| --- | --- | --- | --- | --- |
| ALIGN-2 | Runtime `llm` branch-close scope policy is now locked (automatic solver-to-LLM fallback is explicitly deferred, not implicit) | M | Darren/Codex | Keep this decision documented across Prompt/Plan/contract docs; only reopen if product scope changes |
| ALIGN-7 | Runtime reasoning remains solver-first + captured/manual-live LLM by design; automatic solver-to-LLM fallback is intentionally deferred outside this branch-close boundary | M | Darren/Codex | Treat fallback as a future feature branch; do not silently reintroduce implicit fallback in this branch |
| ALIGN-4 | Duplicate adapter implementations remain in `adapters-cli` and `adapters-web` | M | Darren/Codex | Consolidate shared logic and keep env wrappers thin (U4) |
| ALIGN-5 | Node/browser equivalence coverage is complete for command-hosted surfaces and runtime run/replay/inspect now include runtime decision capture telemetry | L | Darren/Codex | Maintain coverage while finishing remaining branch-close items |

## 6) Deferred Work

- None. Remaining items stay in active branch scope until this branch is complete.

## 7) Merge Readiness

- [x] Branch-close gate items 1-5 explicitly complete and logged.
- [x] Prompt scope and acceptance criteria satisfied.
- [x] Plan milestones complete.
- [x] Implement runbook validations complete.
- [x] Decision log up to date.
- [x] Required docs updated.
