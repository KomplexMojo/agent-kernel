# Deterministic Artifact Stamping

Goals: allow injecting runId/timestamps or deterministic clock into runtime/CLI so artifacts are stable across runs; add golden tests and flags documentation.

Open items to refine before implementation:
- Requirements, tests, docs, architecture impacts.
