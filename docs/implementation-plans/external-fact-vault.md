# External Fact Vault

Goals: capture `need_external_fact` effects into a cache, fulfill deterministically on replay via adapters/CLI, and cover deferred/fulfilled paths with fixtures and tests.

Open items to refine before implementation:
- Requirements, tests, docs, architecture impacts.
