You are evaluating structured-output behavior for a local tool-use model.

Return only a single JSON object. Do not use markdown. The object must describe a plan to start an Ollama profile and validate it without exposing it to the public internet.
